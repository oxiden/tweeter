<% content_for :top do %>
Shops
<% end %>

<%= form_for(@search, :url => shops_path, :html => {:method => :get}) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name_contains %>
  <%= f.submit 'search' %>
<% end %>

<table>
  <tr>
    <th>name</th>
    <th></th>
    <th></th>
  </tr>

<% @shops.each do |shop| %>
  <tr>
    <td><%= shop.name %></td>
    <td><%= link_to 'Show', shop_menus_path(shop) %></td>
    <td><%# link_to 'Edit', edit_shop_path(shop) %></td>
  </tr>
<% end %>
</table>

<br />

<%# link_to 'New Shop', new_shop_path %>
<%= paginate @shops %>
